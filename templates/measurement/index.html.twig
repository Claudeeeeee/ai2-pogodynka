{% extends 'base.html.twig' %}

{% block title %}Measurement Index{% endblock %}

{% block body %}
    <h1>Measurement Index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Celsius</th>
                <th>City</th>
                <th>Date</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for measurement in measurements %}
            <tr>
                <td>{{ measurement.id }}</td>
                <td>{{ measurement.celsius }}</td>
                <td>{{ measurement.weatherRecord.location.city }}</td>
                <td>{{ measurement.weatherRecord.date ? measurement.weatherRecord.date|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('app_measurement_show', {'id': measurement.id}) }}">show</a>
                    <a href="{{ path('app_measurement_edit', {'id': measurement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_measurement_new') }}">Create new</a>
{% endblock %}

